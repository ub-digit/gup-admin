---
- name: Pull backend database
  hosts: all
  roles:
  - role: ub-ansible-deploy-export-db
    vars:
      db_service: admin-db
      database_variant: postgres
      db_dump_filename: backend-database.sql
